#include <iostream>

int main()
{
    int i; // Khai báo bi?n i có ki?u d? li?u s? nguyên (integer)
    float f; // Khai báo bi?n f có ki?u d? li?u s? th?c (float)
    char c; // Khai báo bi?n c có ki?u d? li?u kı t? (character)

    int a, b; // Khai báo 2 bi?n a, b có ki?u d? li?u s? nguyên (integer)

    int d = 1; // Khai báo bi?n d và gán (assign) cho bi?n d giá tr? 1

    std::cout << d;

    return 0;
}
